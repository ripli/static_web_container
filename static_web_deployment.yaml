apiVersion: apps/v1
kind: Deployment
metadata:
  name: static_web_deployment
  labels:
    app: nginx
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
      - name: static_web_deployment
        image: prili/static_web:latest
        ports:
        - containerPort: 80
