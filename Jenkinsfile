pipeline {
    agent any
    stages {
    	stage('Build') {
    	    steps {
    		    sh 'echo "build-test"'
    	    }
    	}
    	stage('Deploy') {
    	    steps {
    		    sh 'echo "deploy-test"'
    	    }
    	}
    }
}

